# these are localhost because when we're using the web app we're on the host
# and not in the docker network
API_BASE_URL=http://localhost:42070
WS_URL=ws://localhost:42070
API_CHUCKER_URL=http://localhost:42069

ENABLE_WHATSAPP=false

# Google oauth
GOOGLE_OAUTH_CLIENT_ID="google-oauth-client-id"
OAUTH_GOOGLE_URL="https://accounts.google.com/o/oauth2/v2/auth"